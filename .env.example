# Copy this file to .env and set a real value to pin a fixed API key
# across container restarts. Left unset, docker-compose passes an empty
# string through and the service falls back to generating a random key
# at startup (logged once) -- see SHADOW_RUNNER.md.
SHADOW_RUNNER_API_KEY=
